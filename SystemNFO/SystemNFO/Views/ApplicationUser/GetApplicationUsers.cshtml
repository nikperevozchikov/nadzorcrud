@model IEnumerable<SystemNFO.Models.ApplicationUser>

<p>
    @Html.ActionLink("Добавить куратора", "Create")
</p>
<h2>Кураторы</h2>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fio)
        </th>
   
    
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fio)
        </td>
      
        <td>
            @Html.ActionLink("Выбор организации", "Edit", new { id=item.Id })
         
    </tr>
}

</table>
