@model IEnumerable<SystemNFO.Models.OrgCur>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ogrn)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Socrnameorg)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ogrn)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Socrnameorg)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.OrgCurId }) |
                @Html.ActionLink("Details", "Details", new { id=item.OrgCurId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.OrgCurId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
